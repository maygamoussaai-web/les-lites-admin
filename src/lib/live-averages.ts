/**
 * @deprecated Retiré — les moyennes viennent uniquement des formules du modèle Excel
 * (voir xlsx-writeback + xlsx-formula). Ce fichier est conservé vide pour éviter les
 * imports cassés pendant le déploiement.
 */
export {};
